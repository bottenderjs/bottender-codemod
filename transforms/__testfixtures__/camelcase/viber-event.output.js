async function App(context) {
  context.event.rawEvent.messageToken;
  context.event.rawEvent.sender.apiVersion;
  context.event.rawEvent.message.trackingData;

  context.event.message.trackingData;
}
